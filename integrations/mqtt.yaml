---
# MQTT (aka MQ Telemetry Transport) is a machine-to-machine or “Internet of Things”
# connectivity protocol on top of TCP/IP. It allows extremely lightweight publish/subscribe
# messaging transport.
#
# https://www.home-assistant.io/integrations/mqtt/
#
mqtt: 
  sensor: !include_dir_list ../entities/mqtt/sensor
  #switch: !include_dir_list ../entities/mqtt/switches
  water_heater: !include_dir_list ../entities/mqtt/water_heater